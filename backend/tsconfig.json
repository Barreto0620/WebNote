{
  "compilerOptions": {
    "target": "es2016",                       /* Especifica a versão alvo do ECMAScript para o código de saída. */
    "module": "commonjs",                     /* Especifica o sistema de módulos para o código gerado. */
    "rootDir": "./src",                       /* Especifica o diretório raiz das entradas. */
    "outDir": "./dist",                       /* Especifica o diretório de saída para os arquivos compilados. */
    "esModuleInterop": true,                  /* Habilita a interoperabilidade padrão para módulos CommonJS e ES. */
    "forceConsistentCasingInFileNames": true, /* Garante que os nomes dos arquivos importados e os nomes dos arquivos no disco tenham o mesmo "case". */
    "strict": true,                           /* Habilita todas as opções estritas de verificação de tipo. */
    "skipLibCheck": true                      /* Pula a verificação de tipo de todos os arquivos de declaração (.d.ts). */
  },
  "include": ["src/**/*.ts"],                 /* Inclui todos os arquivos .ts na pasta src e suas subpastas. */
  "exclude": ["node_modules"]                 /* Exclui o diretório node_modules da compilação. */
}